# Rates JSON Host

This repository serves a single static JSON file that the Cost Manager front-end uses for currency conversion.

## Use in the App
- Copy the deployed URL (ending with `/rates.json`) into the “Exchange Rates URL” field in the Cost Manager app settings, then save and test fetch.